#include <stdio.h>

void average(int numberss[], int amount, double *avg)
{
    int i;
    int sum = 0;

    for (i = 0; i < amt; i++) {         
        sum = sum + nums[i];
    }
        
    return ((double) sum / amt);            
}
